var models = require('../models');
// console.log('models:', models);

var Pila = require('../models').pila;

console.log('Pila.findByName:', Pila.findByName);

// var pila = Pila.build({name: 'Home Music'});
//
// console.log('pila:', pila);
